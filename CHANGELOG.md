# 更新记录

## 1.1.2 — 2026-09-09

- 项目统一命名为 GAMForge · GAM 原生重编译器，仓库更名为 HelloClyde/gamforge。
- Windows GUI / CLI 更名为 GAMForge.exe / GAMForge-CLI.exe，ZIP 与全套包目录同步更名。
- 保留内部 a9288 模块、a9288 命令别名、A9288 环境变量和用户配置目录，已有设置无需迁移。
- 新增 gamforge 命令入口；不改游戏编译逻辑、资源格式或安装目录。

## 1.1.1 — 2026-09-09

- 新增全套离线分发包：所需 9288 SDK 编译头文件、A 系列固件、S1C33 LLVM 和 app-local VC 运行库随包提供，不含游戏。
- 冻结 GUI/CLI 自动识别 EXE 旁的 dependencies；移动目录后失效的旧设置自动回退到包内依赖。
- 全套打包要求显式确认 SDK/固件分发授权，记录各依赖哈希与工具链版本；隔离配置下验证冻结入口解析包内路径。

## 1.1.0 — 2026-09-09

- 外置 RES 固定安装到 `A:\系统\数据\`；EXE 仍安装到 `A:\系统\程序\`。

- 增加可选 EXE＋外置 RES 模式：GUI 选项、命令行 `--external-resources` 和配置记忆；默认仍为单文件。
- 4 KiB 校验分页、128 KiB 固定缓存、最多 256 KiB 会话写时复制；不再整份分配外置 GAM。
- 配套资源版本/内容标识与逐页校验，异常读取中止并恢复应用退出流程；发布检查拒绝覆盖不同内容的同名资源。
- 游戏资源禁止进入源码索引和翻译器 ZIP；增加文件格式、缓存换页、写入和错误回归。

## 1.0.0 — 2026-09-07

首个正式编号版本：提供 Windows 图形界面和命令行，将 C6502 GAM 在 PC 上离线翻译为独立 9288 原生 EXE。以下改进集中于共享运行库，不依赖单个游戏的代码补丁。版本号不代表所有 GAM 或完整游戏流程均已验证。

- CLOCK-GUARD-1：CTM 日历/分频器快照增加回绕复核和单调性检查，拒绝倒退读数并限次重试；Timer 独立拒绝负时差，避免 −1 tick 被误作 65536 轮补算而阻塞按键。合法长间隔采用精确 32 位分拆运算，最多 32 轮；保留正常计数器回绕、IRQ/消息合并语义及动画参数，增加异常读数和补算轮数日志。
- BLIT-RAM-BANK-1：修复普通 RAM bank 被统一误判成 LCD 重叠的问题；三国霸业 `$4800` 离屏整图改用行批量输出。按物理区间验证别名，保留真实 LCD 重叠、非连续 bank、MMIO 和回绕的慢路径，不修改游戏菜单、输入或 Timer 规则。
- KEY-CHECKPOINT-1：保持 KEY-MATRIX-1 防串键规则，在已有 C 桥每 32 次调用及内存复制/填充每 128 字节增加只采集检查点；增加分键入队/消费、扫描空档分布与有界事件日志。真机 578 ms 扫描空档是风险证据，不将偶发漏键武断归因于矩阵滤波。
- KEY-MATRIX-1：私有键盘扫描增加释放行、稳定读取、双样本与动作键独立复核，修复可由残留列信号触发的 Right→Exit 串键风险；增加受控滞后/毛刺回归与最近 8 项轻量扫描记录，真机待复测。
- NATIVE-ALU-1：13 个通用比较、算术、移位、编译器临时量和普通 RAM 读改写 helper 改为共享寄存器 S1C33 leaf，移除成功路径的完整 C 桥接。
- 保留参数寄存器和固件 DP，副作用地址先检查再回退；增加 724,800 组目标机器码等价探针。性能构建关闭重采样，不改动画延时和输入逻辑。
- EXIT-INPUT-2：退出时等待物理按键松开，收尾消息泵过滤 KEYDOWN/CHAR/KEYUP，保留按 HWND 过滤的 Timer、绘制及两边界桌面恢复流程。
- 确认/退出首按立即响应，松开稳定 4 个 CTM Tick（约 16 ms）后才重新触发；合并两处物理退出键别名，增加动作键抖动计数。
- 性能日志先写 NATIVE.TMP，成功关闭后轮换 NATIVE.LOG / NATIVE.BAK，只保留最近两次完整会话；增加会话起止时钟，避免异常重开覆盖唯一诊断数据。
- 修复公共 GuiGetMsg 在持续 Timer 过载时无限饿死按键：保留首次 Timer 优先，已让行键下次必须送出，保留 pending Timer 并记录仲裁次数。
- 诊断窗口只生成最终可保留的 64 项，避免异常大时差时反复填充即将淘汰的记录。
- 公共运行库增加 PERF 5 帧间隔诊断：互斥耗时分类、10 秒窗口、整屏图片/输入显示批次区分及有界事件记录。
- 增加 Timer 产生/送达/合并计数，保留最新 NATIVE.LOG；不改变游戏等待参数或直绘策略。
- 公共输入层增加短按 FIFO、绘图内采样，避免阻塞式 GUI 等待造成漏检；方向连发不积压，确认/退出不连发。
- 按真实 A 系列固件恢复 GuiGetMsg 的 Timer 优先级，补充键盘队列、采样间隔与溢出日志及回归测试。
- 绘图和游戏显存写入直接映射到 9288 framebuffer，保留 159×96 到 318×192 的居中 2× 显示。
- 移除局部位图绘制后的定时整屏提交；支持折行显存、bank/DATA 别名和系统覆盖后的恢复。
- 增加直接写入量与修复扫描统计，以及显存地址、像素、动画和背景恢复回归。
- 修复 C6502 32 位左右移的间接参数 ABI，避免资源 bank 计算错误导致启动白屏。
- 补齐零位移、超宽位移、临时区重叠和寄存器/标志位的原固件等价测试。

## 0.1.0

- 从 GAM4980 项目拆出独立的 PC 原生编译器。
- 将恢复、语义分析、S1C33 后端、链接封装和 PC 参考工具分层。
- 增加 Windows 图形界面、GBK 名称、四灰阶图标和转换进度。
- 移除个人目录依赖，分发包使用独立配置、工作目录和冻结子进程协议。
- 增加公开测试、可选固件等价测试、Windows 构建与 tag 发布 workflow。
- 保持实验性兼容性定位，不宣称游戏全流程或全速运行已经验证。
